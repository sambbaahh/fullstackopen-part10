import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  textInput: {
    borderWidth: 1,
    padding: 10,
    borderRadius: 5,
  },
  textInputInvalid: {
    borderColor: "#d73a4a",
  },
  errorText: {
    paddingVertical: 0,
    marginTop: -5,
    marginBottom: 5,
  },
});

export default styles;
